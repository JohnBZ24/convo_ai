# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# env — .env.example is committed, real .env never is
.env
.env.*
!.env.example

# expo / react native (native dirs are generated by prebuild)
.expo/
android/
ios/
*.jks
*.keystore

# worklets bundle mode watch folder
.worklets/

# os / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# TanStack Router generates this on every dev/build
apps/server/src/routeTree.gen.ts
